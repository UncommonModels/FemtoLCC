# Per-target build directories, each with its own sdkconfig.
build-*/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Working notes on porting OpenMRN to the C6, not a deliverable.
UPSTREAM-C6.md
